{
  "name": "agent",
  "description": "Generic baseline for an autonomous coding/ops agent, for the openai/anthropic/langchain/otel adapters. Grants the everyday capabilities such an agent legitimately needs; anything beyond it - cloud/IAM, mail, an unexpected database, off-allowlist egress - accretes and shows as drift. Tighten it to your agent's real job for sharper detection.",
  "capabilities": [
    "fs:read",
    "fs:write",
    "fs:search",
    "exec:local",
    "proc:spawn",
    "net:egress:read",
    "state:read",
    "state:write",
    "agent:spawn",
    "tool:*"
  ],
  "egress": {
    "mode": "open_read",
    "allow": []
  },
  "budget": {
    "max_distinct_hosts": 20,
    "max_probe_ratio": 0.5,
    "max_instances": 4
  }
}
